# Core runtime dependencies for MLX VLM Check
# Matches pyproject.toml [project.dependencies]
# 
# Optional extras (transformers, mlx-lm, psutil, tokenizers, einops, num2words)
# are in pyproject.toml [project.optional-dependencies.extras]
# Install with: pip install -e ".[extras]"

# Core MLX dependencies
mlx>=0.29.1
mlx-vlm>=0.0.9

# Image processing
Pillow>=10.3.0

# Utilities
huggingface-hub>=0.23.0
tabulate>=0.9.0
tzlocal>=5.0
