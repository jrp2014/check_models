#
# This file is autogenerated by pip-compile with Python 3.13
# by the following command:
#
#    pip-compile --output-file=requirements.txt requirements.in
#
# Slimmed to core runtime dependencies only.
# Optional extras (transformers, mlx-lm, psutil) are in pyproject.toml [project.optional-dependencies.extras]
#
# Core MLX dependencies
mlx>=0.29.1
mlx-vlm>=0.0.9

# Image processing
pillow>=10.3.0

# Utilities
huggingface-hub>=0.23.0
tabulate>=0.9.0
tzlocal>=5.0

# The following are transitive dependencies (auto-resolved by pip):
# - certifi, charset-normalizer, filelock, fsspec, requests, tqdm, typing-extensions
#   (from huggingface-hub)
# - backports.zoneinfo (from tzlocal on Python <3.9)
# - pytz (from tzlocal)
# - Various mlx-vlm transitive deps
