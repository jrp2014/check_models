repos:
repos:
  - repo: local
    hooks:
      - id: quality-checks
        name: Quality Checks (Ruff, MyPy, Tests, etc.)
        entry: src/tools/run_quality_checks.sh
        language: system
        pass_filenames: false
        always_run: true
