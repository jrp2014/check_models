# Core runtime dependencies for MLX VLM Check
# This file is compiled to requirements.txt using pip-tools
# 
# Update: edit this file, then run: make lock-deps
# Install: pip install -r requirements.txt
#
# Note: Optional extras (psutil, tokenizers, einops, num2words, mlx-lm, transformers)
# are defined in pyproject.toml [project.optional-dependencies.extras]
# Install with: pip install -e ".[extras]"

# Core MLX dependencies
mlx>=0.29.1
mlx-vlm>=0.0.9

# Image processing
Pillow>=10.3.0

# Utilities
huggingface-hub>=0.23.0
tabulate>=0.9.0
tzlocal>=5.0
